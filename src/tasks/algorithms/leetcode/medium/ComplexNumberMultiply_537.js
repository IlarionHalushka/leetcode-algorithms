var complexNumberMultiply = function(a, b) {
    // parse strings indexOf(+)
    // first and second parts
    // first2 and second2

    // resultFirstpart = first * first2 - second * second2

    // result second part = first * second2 + first2 * second

    // return resultFirst + '+' + resultSecond + 'i'
};